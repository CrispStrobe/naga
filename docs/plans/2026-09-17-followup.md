# Naga completion and continuous verification plan

> **For Hermes:** Execute bounded independent tasks with tests first and final combined verification.

**Goal:** Close the optimization work, give filled boards a victory rule, automate regression checks, extend end-to-end lifecycle coverage and measure available hardware honestly.

**Architecture:** Keep mode policy explicit. Use a finite free-cell selector and terminal victory state where food exhaustion means completion. Keep production VS AI strategy; remove the unused shared AI implementation while retaining its difficulty enum in a neutral file. Run browser checks against the actual WASM build.

**Tech stack:** Flutter 3.44.4/Dart 3.12.2, Flame, flutter_test, GitHub Actions, Playwright.

## Completed baseline
- Full tests and analysis passed immediately before follow-up.
- Four local commits preserve prior optimization, sessions/tests, dependency cleanup and evidence.
- CLAUDE.md update was approved and applied on renewed user request.

## 1. Full-board completion
Read SnakeGame and sibling spawning loops, write finite-selector and filled/nearly-filled board regression tests before implementation. Add explicit hasWon/onVictory lifecycle and registry result routing; prevent terminal state from issuing duplicate callbacks, reset correctly. Preserve normal scoring and extra lives; victory does not consume a life. Apply exhaustion handling to compatible grid games without replacing level-clear rules. Run focused and full tests.

## 2. Continuous verification
Inspect existing workflows. Add least-privilege PR/push/manual check workflow with Flutter pinned 3.44.4, analysis/unit tests and WASM browser matrix. Pin Node/Playwright and install actual browser dependencies. Upload logs/screenshots even on failures. Exercise workflow commands locally and validate syntax. Do not trigger production deployment merely to test CI.

## 3. Live lifecycle and touch
Extend scripts/qa with deterministic checks for death/restart/menu, score/lives, persisted settings, Duel player inputs and touch taps/swipes. Use real browser interactions, not fabricated API responses. Use unit/widget tests to cover deterministic states inaccessible reliably from random live play. Retain JSON evidence and distinguish actual checks from gestures sent without assertions.

## 4. Remove unused AI
Trace imports. Move AiDifficulty to a small neutral definition; delete dead SnakeAI, its tests and redundant benchmark references, retain production VS AI trace/performance tests. Verify analyzer/tests and no lingering imports.

## 5. Device/release measurements
flutter devices found local macOS, Chrome and wireless iPad (iOS 26.3.1). Investigate whether physical Safari Web Inspector is actually accessible before promising on-device WASM measurements. Prefer non-destructive web profiling, no silent replacement of installed apps. Measure Chrome release WASM startup/frame/heap/network under documented conditions as a separate fallback; never label throttled desktop as a real phone. Record blocked physical tests explicitly if device unlock/inspection is needed.

## 6. Final gate and commits
Run all tests/analyze, build WASM, run extended browser checks, verify exact result counts and screenshots. Read-only review before final commit. Commit each cohesive follow-up with tests. Do not push main: existing push triggers production deploy and user only approved local commits. Report physical-device or CI-remote limitations clearly.
